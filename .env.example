# ─── Model ────────────────────────────────────────────────────────────────
# "mock"    → canned streaming replies, no network. Use this while fine-tuning.
# "openai"  → any OpenAI-compatible /v1/chat/completions endpoint.
#             Works with vLLM, SGLang, Nebius, Together, Fireworks, Ollama.
MODEL_PROVIDER=mock

# Your fine-tuned Qwen2.5-VL-7B deployment
MODEL_BASE_URL=https://api.studio.nebius.com/v1
MODEL_API_KEY=
MODEL_NAME=padhle/qwen2.5-vl-7b-cbse10

# Optional vision fallback (Llama-3.2-11B-Vision) for diagram-heavy questions
VISION_MODEL_NAME=meta-llama/Llama-3.2-11B-Vision-Instruct

# ─── Retrieval ────────────────────────────────────────────────────────────
# "memory" → ships with seed chunks so the UI renders citations today.
# "qdrant" | "pgvector" → implement in lib/rag/vectorstore.ts
RAG_PROVIDER=memory
QDRANT_URL=
QDRANT_API_KEY=
QDRANT_COLLECTION=ncert_2026
DATABASE_URL=

# Embeddings for the corpus. Any OpenAI-compatible /v1/embeddings endpoint.
EMBEDDINGS_PROVIDER=mock
EMBEDDINGS_BASE_URL=
EMBEDDINGS_API_KEY=
EMBEDDINGS_MODEL=BAAI/bge-m3

# Academic year the corpus is pinned to. NCERT changes every year — the
# retriever hard-filters on this so last year's text never leaks into answers.
NCERT_YEAR=2026-27
